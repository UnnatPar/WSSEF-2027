model:
  d: 256
  L: 6
  k: 8
  mask_ratio: 0.25

training:
  batch_size: 256
  lr: 1.0e-3
  weight_decay: 0.05
  epochs: 100
  grad_clip: 1.0
  num_workers: 8

data:
  max_pulses: 256
  batch_dir: data/train/
  meta_path: data/train_meta.parquet
  geometry_path: data/sensor_geometry.csv
  train_batches: [1, 50]

logging:
  project: neutrinojepa
  run_name: pretrain_mae_v1

checkpoint:
  dirpath: checkpoints/pretrain_mae_v1/
  filename: "epoch{epoch:04d}-step{step:09d}"
